//If this script is attached to something, then after the scene restarts or so the object that this script is attached to will not be destoryed and will stay there.

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KnifeFlip
{
public class DontDestroyOnLoad : MonoBehaviour {

		/*void Awake() {
			DontDestroyOnLoad(transform.gameObject);
		}
		*/
	}
}